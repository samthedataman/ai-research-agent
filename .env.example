# ── Telegram (required) ──────────────────────────────────────────────────────
# Get a token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# ── LLM Provider ─────────────────────────────────────────────────────────────
# "ollama" (free, local) or "openrouter" (cloud, cheap)
LLM_PROVIDER=ollama

# Ollama (local) — install from https://ollama.ai
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# OpenRouter (cloud) — get a key from https://openrouter.ai
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-chat

# ── Optional API Keys ────────────────────────────────────────────────────────
# All collectors work without API keys. These just unlock extras.
RAPIDAPI_KEY=
GITHUB_TOKEN=
SERPER_API_KEY=
BRAVE_API_KEY=

# ── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL=sqlite+aiosqlite:///./bot.db

# ── Data Sources ─────────────────────────────────────────────────────────────
# Comma-separated list of active collectors
# Available: news,weather,crypto,dexscreener,reddit,github,arxiv,stocks,wikipedia,ddg,ddg_news,serper,tmz,cryptonews
ACTIVE_COLLECTORS=news,reddit,arxiv,crypto,github,weather,stocks,wikipedia,ddg,ddg_news,tmz,cryptonews

# Collector defaults (used when no query is provided)
WEATHER_LOCATIONS=New York,San Francisco,London
STOCK_SYMBOLS=AAPL,GOOGL,MSFT,NVDA,TSLA
REDDIT_SUBREDDITS=technology,machinelearning,artificial
CRYPTO_COINS=trending

# ── Logging ──────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
